# Stories for SPEC-003: Framework Tooling & Upgrade
# Agents may only modify 'status' and 'verified_by' fields

stories:
  STORY-001:
    name: Minimal AGENTS.md redesign
    description: Redesign AGENTS.md as generic boot loader — strip project content, keep framework workflow, add discovery pointers for project config and skills
    status: failing
    verified_by: null

  STORY-002:
    name: pyproject.toml + UV setup
    description: Add pyproject.toml with UV, Click dependency, tools/ directory structure, and committed uv.lock
    status: failing
    verified_by: null

  STORY-003:
    name: Framework/project directory boundary
    description: Formalize and document the convention that framework files live in known directories, project files live elsewhere, and upgrade replaces framework directories wholesale
    status: failing
    verified_by: null

  STORY-004:
    name: /upgrade skill
    description: Create skill that replaces framework directories from upstream — nuke-and-replace with pre-upgrade commit for rollback safety
    status: failing
    verified_by: null

verification:
  criteria:
    - AGENTS.md is generic — no project-specific content, discovers project context from known locations
    - pyproject.toml valid and UV resolves dependencies
    - Framework/project boundary convention is documented and directory structure matches
    - /upgrade replaces framework directories from upstream without touching project files
    - /upgrade auto-commits before replacing for rollback safety
    - /upgrade warns if custom files detected in framework directories
    - All new skills have CSO-compliant descriptions
    - All new skills include anti-rationalization tables
  completed: false
  completed_date: null
